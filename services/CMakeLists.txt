# Application-type DMOD modules that live under dmnet/services/.
#
# Nothing here yet - networkd/ (the future lwIP-based network daemon that
# will drive dmnetif-registered interfaces) has no module scaffold yet.
# Add `add_subdirectory(networkd)` once it does.
