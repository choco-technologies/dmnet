/**
 * @file dmnetif_test.c
 * @brief Test steps for dmnetif
 *
 * Empty for now - dmnetif.c is still a stub (see its file comment), so
 * there is no real registry/device behavior to assert on yet. Real
 * DMOD_TEST_STEP()s land alongside the real implementation.
 */
#include "dmod_test.h"
#include "dmnetif.h"
