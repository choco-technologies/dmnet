# Application-type DMOD modules that live under dmnet/tools/.
#
# Nothing here yet - netctl/ and ifconfig/ have no module scaffold yet.
# Add `add_subdirectory(netctl)` / `add_subdirectory(ifconfig)` once they do.
